Login con passport y ts-node

-D @types/node @types/express, @typesmodules

typescript:
    npm i -g typescript
    tsc --init (ts conf on project)
    tsconfig.json:
        "outDir": "./dist" //salida
        "rootDir": "./src" //entrada
        "moduleResolution": "node"